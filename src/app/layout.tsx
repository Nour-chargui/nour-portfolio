import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nour CHARGUI | Cybersecurity Engineer Portfolio",
  description: "Personal portfolio of Nour CHARGUI, a Cybersecurity Engineering Student specializing in AI-driven security and software engineering.",
  keywords: ["Cybersecurity", "Engineering", "Portfolio", "Nour CHARGUI", "AI Security", "Web Development"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
